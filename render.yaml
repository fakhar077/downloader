services:
  - type: web
    name: safe-video-downloader
    env: node
    plan: free
    buildCommand: "pip install yt-dlp"
    startCommand: "node src/server.js"
    healthCheckPath: "/"
    envVars:
      - key: PORT
        value: 3000
